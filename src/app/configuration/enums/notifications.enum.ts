// This is an export statement that makes the enum available for other modules to import
export enum NotificationEnum {
  // The enum is called NotificationEnum and contains a single value called ResponseConcernNotification
  ResponseConcernNotification = 'A new message has been replied to your concern',
}
